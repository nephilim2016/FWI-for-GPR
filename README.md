# FWI-for-GPR
For GPR full waveform inversion (A Scan), dual-parameter FWI (sigma and epsilon) based on the L-BFGS optimization algorithm, using pure python language with CPU parallel computing to improve calculation efficiency.
